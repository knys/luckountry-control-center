import test from"node:test";import assert from"node:assert/strict";import{Readable}from"node:stream";import type{IncomingMessage,ServerResponse}from"node:http";import{mkdtemp,rm}from"node:fs/promises";import{join}from"node:path";import{tmpdir}from"node:os";import{DurableSelfCommissioningStore,SelfCommissioningOrchestrator,type CommissioningExecutors}from"../src/application/self-commissioning.js";import{ProductionSelfCommissioningControl}from"../src/application/production-self-commissioning.js";import{createRequestHandler}from"../src/http-app.js";
async function fixture(){const dir=await mkdtemp(join(tmpdir(),"production-wiring-")),path=join(dir,"runs.json"),calls:string[]=[],reports:string[]=[];let release!:()=>void;const gate=new Promise<void>(r=>release=r),store=await DurableSelfCommissioningStore.open(path),executors:CommissioningExecutors={tx:async r=>{calls.push(r.operation);return{status:"SUCCEEDED",summary:"tx",evidence:[]}},gtx:async r=>{calls.push(r.operation);return{status:"SUCCEEDED",summary:"gtx",evidence:[]}},codex:async()=>{calls.push("CODEX_JOB");await gate;return{status:"SUCCEEDED",summary:"codex",evidence:[]}}},orchestrator=new SelfCommissioningOrchestrator(store,executors),control=new ProductionSelfCommissioningControl(store,orchestrator,{report:async run=>{reports.push(run.status)}},async()=>true,true,()=>"fixed");return{dir,path,calls,reports,store,control,release,cleanup:()=>rm(dir,{recursive:true,force:true})}}
test("production profile is fixed, starts an actual actor, dispatches fixed transports and terminates",async t=>{const f=await fixture();t.after(f.cleanup);await assert.rejects(f.control.create({profile:"ARBITRARY",objective:"shell",command:"pwsh"}),/allowlisted/);const created=await f.control.create({profile:"LCC008_REAL_ACCEPTANCE"});assert.equal(created.status,"QUEUED");const started=await f.control.start(created.runId);assert.equal(started.status,"RUNNING");assert.ok(started.activeActor);assert.ok(started.activeExecutionId);while(!f.calls.includes("CODEX_JOB"))await new Promise(r=>setImmediate(r));assert.deepEqual(f.calls.slice(0,6),["TX_LCC_STATUS","TX_LCC_MIGRATE_PILOT_STORE","TX_LCC_RECONCILE","GTX_WORKER_STATUS","GTX_WORKER_DESCRIPTOR","GTX_WORKSPACE_PREFLIGHT"]);f.release();while(!f.reports.includes("SUCCEEDED"))await new Promise(r=>setImmediate(r));assert.equal((await f.store.get(created.runId))?.status,"SUCCEEDED")});
test("duplicate start does not duplicate dispatch and restart restores queued checkpoint",async t=>{const f=await fixture();t.after(f.cleanup);const created=await f.control.create({profile:"LCC008_REAL_ACCEPTANCE"}),[a,b]=await Promise.all([f.control.start(created.runId),f.control.start(created.runId)]);assert.equal(a.activeExecutionId,b.activeExecutionId);assert.equal(f.calls.filter(v=>v==="TX_LCC_STATUS").length,1);f.release();while((await f.store.get(created.runId))?.status!=="SUCCEEDED")await new Promise(r=>setImmediate(r));const restored=await DurableSelfCommissioningStore.open(f.path);assert.ok((await restored.list()).length===1)});
test("cancel remains terminal even when active actor later returns",async t=>{const f=await fixture();t.after(f.cleanup);const created=await f.control.create({profile:"LCC008_REAL_ACCEPTANCE"});await f.control.start(created.runId);const cancelled=await f.control.cancel(created.runId),count=f.reports.filter(v=>v==="CANCELLED").length;assert.equal(cancelled.status,"CANCELLED");f.release();while(f.reports.filter(v=>v==="CANCELLED").length===count)await new Promise(r=>setImmediate(r));assert.equal((await f.store.get(created.runId))?.status,"CANCELLED")});
async function invoke(handler:ReturnType<typeof createRequestHandler>,method:string,path:string,body="",authorization?:string){const req=Object.assign(Readable.from(body?[body]:[]),{method,url:path,headers:authorization?{authorization}:{}})as unknown as IncomingMessage;let code=0,text="";await handler(req,{writeHead:(v:number)=>code=v,end:(v?:string)=>text=v??""}as unknown as ServerResponse);return{code,body:text}}
test("fixed control API is authenticated for mutation and GET exposes bounded truth fields",async t=>{const f=await fixture();t.after(f.cleanup);const handler=createRequestHandler({getProducts:async()=>({})}as never,[],undefined,undefined,undefined,undefined,undefined,undefined,{control:f.control,token:"control-secret"}),body=JSON.stringify({profile:"LCC008_REAL_ACCEPTANCE"});assert.equal((await invoke(handler,"POST","/api/self-commissioning/runs",body)).code,401);const created=await invoke(handler,"POST","/api/self-commissioning/runs",body,"Bearer control-secret");assert.equal(created.code,201);const listed=await invoke(handler,"GET","/api/self-commissioning");assert.equal(listed.code,200);for(const field of["runId","profile","status","currentStep","activeActor","completedSteps","retryUsage","blocker","humanGate","ballHolder","ongoing"])assert.match(listed.body,new RegExp(field));assert.doesNotMatch(listed.body,/control-secret|environment|stdout|stderr|\/home\//i)});
